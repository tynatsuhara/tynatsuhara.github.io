window.onload = function() {
  configureParallax();

//addParallax(id, img, width, height, weight, left, right, top, bottom) {
  addParallax("mtn", "images/mtn.png", "40%", "auto", -4, "0", "0", "auto", "0");
}
